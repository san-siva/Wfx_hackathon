{
  "name": "my-extension",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild content.ts --bundle --outfile=dist/content.js --format=esm"
  },
  "devDependencies": {
    "esbuild": "^0.14.0",
    "typescript": "^4.0.0"
  }
}
